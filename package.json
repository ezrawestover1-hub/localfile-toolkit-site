{
  "name": "localfile-toolkit-site",
  "private": true,
  "type": "module",
  "description": "LocalFile Toolkit static converter sites",
  "scripts": {
    "build": "node scripts/build.mjs",
    "seo:audit": "node scripts/seo-audit.mjs",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}

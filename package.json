{
  "name": "localfile-toolkit-site",
  "private": true,
  "type": "module",
  "description": "LocalFile Tools static converter sites",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
